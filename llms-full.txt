# akc-cycle — AI Reference

This document is the self-contained AI reference for `akc-cycle` (shimo4228, MIT), the install entry point for the Agent Knowledge Cycle (AKC). It ships two layers: (1) a single behavioral rules file that distills the cycle's six phases — Research, Extract, Curate, Promote, Measure, Maintain — plus Scaffold Dissolution into principles an agent follows through natural conversation, loaded deterministically every session; and (2) a Claude Code plugin carrying the nine cycle-phase skills plus the two subagents they invoke, triggered probabilistically. The rules file is the recommended minimal floor; the plugin adds the skill layer.

> **Audience**: AI search engines and AI agents. Human-facing narrative lives in [README.md](README.md).

## Project Facts

- **Name**: akc-cycle
- **License**: MIT
- **GitHub**: https://github.com/shimo4228/akc-cycle
- **Author**: shimo4228 (shimomoto-tatsuya)
- **Author research**: [AKC](https://github.com/shimo4228/agent-knowledge-cycle) ([DOI 10.5281/zenodo.19200726](https://doi.org/10.5281/zenodo.19200726)), [Contemplative Agent](https://github.com/shimo4228/contemplative-agent) ([DOI 10.5281/zenodo.19212118](https://doi.org/10.5281/zenodo.19212118)), [AAP](https://github.com/shimo4228/agent-attribution-practice) ([DOI 10.5281/zenodo.19652013](https://doi.org/10.5281/zenodo.19652013))
- **Type**: two-layer AKC install target — a Markdown behavioral rules file (loaded deterministically) plus a Claude Code plugin of nine Agent Skills and two subagents (triggered probabilistically)
- **Layout**: `rules/common/akc-cycle.md` (rule) · `skills/<name>/SKILL.md` × 9 + `agents/*.md` × 2 (plugin payload) · `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` (plugin/marketplace manifests)
- **Install (rules file, minimal)**: `cp rules/common/akc-cycle.md ~/.claude/rules/common/akc-cycle.md`
- **Install (plugin, skill layer)**: `/plugin marketplace add shimo4228/akc-cycle` then `/plugin install akc-cycle@akc-cycle`
- **Phases**: 6 — Research, Extract, Curate, Promote, Measure, Maintain — plus Scaffold Dissolution (with its evidence standard) and digests of three AKC worldview mechanisms (expiry-conditioned knowledge ADR-0026, judge/build/human attention topology ADR-0024, LLM-first artifact readability ADR-0025)
- **Editions**: the rules file in this repo is the **self-contained edition** (owned here, no skills assumed, since 2026-09-01); the author's harness runs a separate **pointer edition** delegating each mechanism to installed skills ([claude-harness rules/common/akc-cycle.md](https://github.com/shimo4228/claude-harness/blob/main/rules/common/akc-cycle.md))
- **Plugin skills (phase binding)**: search-first (Research), learn-eval (Extract), skill-stocktake / skill-health / rules-stocktake (Curate), rules-distill (Promote), skill-comply (Measure), context-sync / repo-asset-stocktake (Maintain); subagents adr-writer, codemap-writer
- **Relation of the two layers**: the rules file trades per-phase depth for always-loaded reliability; the plugin skills add the depth. Claude Code plugins cannot ship always-loaded rules, so the rules file is never part of the plugin payload
- **Design lineage**: the install target for the [Agent Knowledge Cycle](https://github.com/shimo4228/agent-knowledge-cycle), whose ADRs hold the judgment lineage
- **Sync model**: canonical copies of the skills and subagents in the author's Claude Code harness (`~/.claude/`); this repo is a one-way mirror for those via `scripts/sync-from-local.sh` (fixed allowlist: 9 skills + 2 agents; aborts on missing component or origin marker). The rules file is not synced — this repo owns its self-contained edition

## What is akc-cycle?

akc-cycle is defined as a single behavioral rules file that installs the Agent Knowledge Cycle into an AI agent's rules directory. The Agent Knowledge Cycle is a six-phase loop by which an agent metabolizes its own working experience into improvable skills, rules, and memory. This file distills those six phases plus a Scaffold Dissolution clause into actionable principles, each with an explicit trigger, so the cycle runs through natural conversation without installing six separate skill repositories. A rules file is loaded every session and shapes behavior deterministically; a skill triggers probabilistically. akc-cycle deliberately chooses the deterministic layer for the cycle as a whole.

## What are the six phases?

The six phases are defined as Research, Extract, Curate, Promote, Measure, and Maintain. Research means search broadly and filter by signal — define the signal (what would change your next action) before the search, then adopt or extend an existing solution before building. Extract means capture reusable patterns from a session, evaluating quality before saving and routing to the right layer (memory / skill / rule). Curate means audit accumulated knowledge for redundancy, staleness, and silence. Promote means elevate recurring patterns to rules once the repetition itself becomes the signal, moving knowledge from the probabilistic layer to the deterministic one. Measure means verify behavioral change quantitatively, checking compliance in tool calls, outputs, and the agent's stated reasoning. Maintain means keep documentation roles clean — every file serves exactly one of Context, Architecture, Decisions, External — and keep each numeric fact in one canonical place.

## How does akc-cycle relate to the six AKC skills?

The relation is defined as two install layers for the same cycle with opposite reliability profiles. The nine cycle-phase skills (search-first, learn-eval, skill-stocktake, skill-health, rules-stocktake, rules-distill, skill-comply, context-sync, repo-asset-stocktake) each give deep "how" guidance for one phase and trigger probabilistically when their description matches the situation. The rules file gives the whole cycle as always-loaded behavioral principles — shallower per phase, but reliably present every session. Installing the rule does not require installing the skills, and vice versa; the rule is the lightweight entry point, the skills are the depth. Since v1.1.0 this repository ships both layers: the rule as a copy-install file and the nine skills (plus the adr-writer and codemap-writer subagents they invoke) as a Claude Code plugin. Each skill also remains available as its own standalone repository.

## How do I install akc-cycle as a Claude Code plugin?

The plugin install is defined as a two-command marketplace flow: `/plugin marketplace add shimo4228/akc-cycle` registers this repository as a marketplace (its `.claude-plugin/marketplace.json` lists one plugin whose source is the repository root), and `/plugin install akc-cycle@akc-cycle` installs the nine skills and two subagents. Claude Code plugins can carry skills, agents, hooks, and MCP servers, but not always-loaded rules — so the plugin deliberately excludes `rules/common/akc-cycle.md`, which must be copy-installed separately (Path 1). The plugin adds no hooks, MCP servers, or settings: the payload is skills and their subagents only, keeping the scaffold minimal by design.

## What is Scaffold Dissolution?

Scaffold Dissolution is defined as the intended end state in which the rules become unnecessary because the cycle has been internalized. The rules are scaffolding, and success is measured not by rule count but by whether the cycle runs naturally without explicit invocation. Dissolution has two vectors: inward, where a principle is absorbed into the agent's and operator's conversation patterns so the rule that taught it is no longer needed; and downward, where the official harness evolves to handle a domain natively, so a hand-written rule over the same domain becomes a stale, drifting shadow that should be retired rather than left to override newer defaults. A model-generation turnover is a downward trigger. The evidence standard (AKC ADR-0022 / ADR-0023): completion evidence is held-out transfer — behaving the same with the scaffold removed in the same context is only necessary evidence; and artifacts carrying negative information deltas (drifted rules overriding better defaults) are actively deleted rather than shelved, audited explicitly at each model-generation turnover because silence, ablation, and transfer detect only absence. What the harness structurally cannot do is kept; the rest dissolves. The pointer edition running in the author's harness is the worked example of dissolution in progress: the six-phase procedures have moved into installed skills, and the rules file has shrunk to pointers plus the judgment content no skill owns.

## How is akc-cycle kept in sync?

The sync model is defined as a one-way publication mirror for the skill layer only. The canonical copies of the nine skills and the two subagents live in the author's live Claude Code harness under `~/.claude/`; this repository is a downstream mirror updated by `scripts/sync-from-local.sh`, which stages a fixed allowlist (9 skills + 2 agents), validates YAML frontmatter, runs a secret scan, and replaces the managed subtrees without committing — `git diff` is the review gate. The script aborts if any listed component is missing or lacks its `origin: shimo4228` marker, so a published component can never be silently dropped. The rules file is deliberately outside the sync: since 2026-09-01 this repository owns the self-contained edition, while the harness runs a separate pointer edition — the two editions serve different environments (skills absent vs. installed) and are kept distinct on purpose. Because each component has a single canonical source, the AKC research repository no longer carries its own divergent copies; it points to this repository instead.

## Who is shimo4228?

shimo4228 is the author whose three Zenodo-citable research lines are listed in Project Facts above: AKC — a six-phase bidirectional growth loop; Contemplative Agent — autonomous agents grounded in four contemplative axioms; and AAP — harness-neutral accountability ADRs. This repository owns the self-contained edition of the rules file; the skill layer is a one-way publication mirror from the author's live Claude Code harness, synchronized by `scripts/sync-from-local.sh`.
